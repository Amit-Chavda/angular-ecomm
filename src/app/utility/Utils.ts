import { CookieService } from "ngx-cookie-service";
import { cart } from "../entity/cart";

export default class Utils {
    
    constructor(public cookieService:CookieService){}
    static calculateTotalPrice(cart:cart):cart{
        

        return null;
    }

}